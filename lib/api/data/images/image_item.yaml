openapi: 3.0.2
info:
  title: "API"
  version: "1.0.0"
  contact:

paths:

  /{image_id}/metadata.json:
    get:
      summary: Метод для получения информации о фото по id.
      responses:
        "200":
          description: "Успех"

  /asset/{image_id}:
    get:
      summary: Метод для получения списка ссылок для фото в разных разрешениях.
      responses:
        "200":
          description: "Успех"
